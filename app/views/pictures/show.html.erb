<h1>Picture Details</h1>

<%= image_tag(@picture.source, width: 240) %>

<p><%= @picture.caption%></p>

<%= link_to "Toast", picture_url(@picture.id), confirm: "Are you sure?", method: "delete" %>
<%= link_to "Edit", edit_picture_url(@picture.id) %>
